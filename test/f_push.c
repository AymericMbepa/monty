#include "monty.h"

/**
 * f_push - pushes an element to the stack
 * @stack: stack on whitch element will push
 * @line_number: number of elements of stack
 *
 * Return: void
 */
void f_push(stack_t **head, unsigned int line_number)
{
	stack_t *h;
	h = *head;

	if 
}
